---
- name: ensure python3 and pip are installed
  apt:
    name:
      - python3
      - python3-pip
    state: present
    update_cache: true
  become: true

- name: install
  pip:
    name: az-cli
    state: present
    executable: pip3
